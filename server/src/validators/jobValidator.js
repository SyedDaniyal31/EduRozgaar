/**
 * Placeholder validators for request validation (e.g. with express-validator in Phase-2).
 */
export function validateCreateJob(_req, _res, next) {
  next();
}

export function validateUpdateJob(_req, _res, next) {
  next();
}
